import { RoomAllocationViewModel } from '@university-erp/domain-viewmodels';

export interface InfoFieldProps {
  label: string;
  value: string;
  highlight?: boolean;
}

export type { RoomAllocationViewModel };
