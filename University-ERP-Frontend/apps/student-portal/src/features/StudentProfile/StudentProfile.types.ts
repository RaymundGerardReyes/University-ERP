import { StudentProfileViewModel } from '@university-erp/domain-viewmodels';

export interface ProfileFieldProps {
  label: string;
  value: string | number;
}

export type { StudentProfileViewModel };
