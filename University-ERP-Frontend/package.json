{
  "name": "university-erp-frontend",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "libs/*"
  ],
  "scripts": {
    "lint:deps": "depcruise apps libs",
    "audit:all": "npm audit --workspaces --audit-level=high",
    "dev:identity": "dotenvx run -f ../.env -- npm run dev --workspace=apps/identity-portal",
    "dev:student-flow": "concurrently -n \"IDENTITY,APPLICANT,STUDENT\" -c \"magenta,cyan,blue\" \"dotenvx run -f ../.env -- npm run dev --workspace=apps/identity-portal\" \"dotenvx run -f ../.env -- npm run dev --workspace=apps/applicant-portal\" \"dotenvx run -f ../.env -- npm run dev --workspace=apps/student-portal\"",
    "dev:all": "concurrently -n \"IDENT,APPLIC,STUDENT,ADMIN,FACULTY,FINANCE,GOV,LIB,LMS,PLATFORM\" -c \"magenta,cyan,blue,red,yellow,green,gray,blue,yellow,magenta\" \"dotenvx run -f ../.env -- npm run dev --workspace=apps/identity-portal\" \"dotenvx run -f ../.env -- npm run dev --workspace=apps/applicant-portal\" \"dotenvx run -f ../.env -- npm run dev --workspace=apps/student-portal\" \"dotenvx run -f ../.env -- npm run dev --workspace=apps/admin-portal\" \"dotenvx run -f ../.env -- npm run dev --workspace=apps/faculty-portal\" \"dotenvx run -f ../.env -- npm run dev --workspace=apps/finance-console\" \"dotenvx run -f ../.env -- npm run dev --workspace=apps/governance-console\" \"dotenvx run -f ../.env -- npm run dev --workspace=apps/library-portal\" \"dotenvx run -f ../.env -- npm run dev --workspace=apps/lms-web\" \"dotenvx run -f ../.env -- npm run dev --workspace=apps/platform-console\""
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "concurrently": "10.0.4",
    "dependency-cruiser": "^18.1.0",
    "npm-check-updates": "23.0.0"
  }
}
