{
  "name": "university-erp-root",
  "version": "1.0.0",
  "private": true,
  "description": "Root workspace configuration for University ERP Monorepo",
  "scripts": {
    "docker:fresh": "dotenvx run -f .env -- docker compose build --no-cache",
    "docker:up": "dotenvx run -f .env -- docker compose up -d",
    "docker:down": "dotenvx run -f .env -- docker compose down",
    "docker:restart": "npm run docker:restart-backend && npm run docker:restart-frontend",
    "docker:restart-backend": "dotenvx run -f .env -- docker compose restart api worker migrator",
    "docker:restart-frontend": "dotenvx run -f .env -- docker compose up -d --force-recreate frontend-builder",
    "docker:restart-nginx": "dotenvx run -f .env -- docker compose restart nginx",
    "dev:backend": "dotenvx run -f .env -- dotnet run --project University-ERP-Backend/src/Bootstrap/UniversityErp.Api/UniversityErp.Api.csproj",
    "dev:migrator": "dotenvx run -f .env -- dotnet run --project University-ERP-Backend/src/Bootstrap/UniversityErp.Migrator/UniversityErp.Migrator.csproj",
    "dev:frontend": "npm run dev:all --prefix University-ERP-Frontend",
    "env:encrypt": "dotenvx encrypt -f .env",
    "env:decrypt": "dotenvx decrypt -f .env"
  },
  "devDependencies": {
    "@dotenvx/dotenvx": "^1.14.0"
  }
}
