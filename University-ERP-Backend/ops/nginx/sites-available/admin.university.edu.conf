server {
    listen 443 ssl;
    server_name admin.university.edu;

    include snippets/security-headers.conf;
    include snippets/rate-limit.conf;

    ssl_certificate     /etc/nginx/ssl/admin.university.edu.crt;
    ssl_certificate_key /etc/nginx/ssl/admin.university.edu.key;

    location / {
        proxy_pass http://127.0.0.1:4303;
        include snippets/proxy-common.conf;
    }
}

server {
    listen 80;
    server_name admin.university.edu;
    return 301 https://$host$request_uri;
}
