server {
    listen 80;
    listen 3006;
    server_name __PLATFORM_DOMAIN__;
    root /var/www/platform-console;

    include snippets/security-headers.conf;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
