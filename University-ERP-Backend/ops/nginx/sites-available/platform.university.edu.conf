server {
    listen 80;
    server_name platform.university.edu;
    root /var/www/platform-console;

    include snippets/security-headers.conf;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
