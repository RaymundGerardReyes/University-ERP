server {
    listen 80;
    server_name api.university.edu;

    include snippets/security-headers.conf;
    include snippets/rate-limit.conf;

    location / {
        set $api_upstream api:5191;
        proxy_pass http://$api_upstream;
        include snippets/proxy-common.conf;
    }
}
