server {
    listen 443 ssl;
    server_name finance.university.edu;

    include snippets/security-headers.conf;
    include snippets/rate-limit.conf;

    ssl_certificate     /etc/nginx/ssl/finance.university.edu.crt;
    ssl_certificate_key /etc/nginx/ssl/finance.university.edu.key;

    location / {
        proxy_pass http://127.0.0.1:4305;
        include snippets/proxy-common.conf;
    }
}

server {
    listen 80;
    server_name finance.university.edu;
    return 301 https://$host$request_uri;
}
