server {
    listen 80;
    server_name finance.university.edu;
    root /var/www/finance-console;

    include snippets/security-headers.conf;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
