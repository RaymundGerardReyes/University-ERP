server {
    listen 80;
    server_name applicant.university.edu;

    include snippets/security-headers.conf;
    include snippets/rate-limit.conf;

    root /var/www/university-erp/applicant-portal/dist;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location ~* \.(?:css|js|jpg|svg|png|gif|ico|woff2?|eot|ttf|otf)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        access_log off;
    }

    location /api/ {
        include snippets/proxy-common.conf;
        proxy_pass http://api.university.edu;
    }
}
