server {
    listen 80;
    server_name applicant.university.edu;
    root /var/www/applicant-portal;

    include snippets/security-headers.conf;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
