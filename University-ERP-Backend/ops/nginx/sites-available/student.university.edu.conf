server {
    listen 443 ssl;
    server_name student.university.edu;

    include snippets/security-headers.conf;
    include snippets/rate-limit.conf;

    ssl_certificate     /etc/nginx/ssl/student.university.edu.crt;
    ssl_certificate_key /etc/nginx/ssl/student.university.edu.key;

    location / {
        proxy_pass http://127.0.0.1:4301;
        include snippets/proxy-common.conf;
    }
}

server {
    listen 80;
    server_name student.university.edu;
    return 301 https://$host$request_uri;
}
