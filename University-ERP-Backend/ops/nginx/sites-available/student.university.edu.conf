server {
    listen 80;
    server_name student.university.edu;
    root /var/www/student-portal;

    include snippets/security-headers.conf;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
