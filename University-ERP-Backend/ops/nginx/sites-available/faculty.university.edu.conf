server {
    listen 80;
    server_name faculty.university.edu;
    root /var/www/faculty-portal;

    include snippets/security-headers.conf;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
