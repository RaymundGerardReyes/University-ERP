server {
    listen 443 ssl;
    server_name library.university.edu;

    include snippets/security-headers.conf;
    include snippets/rate-limit.conf;

    ssl_certificate     /etc/nginx/ssl/library.university.edu.crt;
    ssl_certificate_key /etc/nginx/ssl/library.university.edu.key;

    location / {
        proxy_pass http://127.0.0.1:4306;
        include snippets/proxy-common.conf;
    }
}

server {
    listen 80;
    server_name library.university.edu;
    return 301 https://$host$request_uri;
}
