server {
    listen 80;
    server_name auth.university.edu;
    root /var/www/identity-portal;

    include snippets/security-headers.conf;
    include snippets/rate-limit.conf;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
