server {
    listen 80;
    listen 5177;
    server_name __GOVERNANCE_DOMAIN__;
    root /var/www/governance-console;

    include snippets/security-headers.conf;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
