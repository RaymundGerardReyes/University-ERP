server {
    listen 80;
    server_name governance.university.edu;
    root /var/www/governance-console;

    include snippets/security-headers.conf;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
