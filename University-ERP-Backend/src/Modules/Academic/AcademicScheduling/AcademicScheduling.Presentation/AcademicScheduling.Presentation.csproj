<Project Sdk="Microsoft.NET.Sdk">
  <ItemGroup>
    <ProjectReference Include="..\AcademicScheduling.Application\AcademicScheduling.Application.csproj" />
    <ProjectReference Include="..\AcademicScheduling.Contracts\AcademicScheduling.Contracts.csproj" />
  </ItemGroup>
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <!-- Add this block so the library knows how to use Web API Controllers -->
<!-- Add this block so the library knows how to use Web API Controllers -->
  <ItemGroup>
    <FrameworkReference Include="Microsoft.AspNetCore.App" />
  </ItemGroup>
</Project>